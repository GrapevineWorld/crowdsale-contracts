pragma solidity ^0.4.21;

//import "truffle/Assert.sol";
//import "truffle/DeployedAddresses.sol";
//import "../contracts/grapevine/token/GrapevineToken.sol";

contract GrapevineTokenTest {
    /*function testInitialBalanceUsingDeployedContract() public {
        GrapevineToken gpv = GrapevineToken(DeployedAddresses.GrapevineToken());

        uint expected = 0;

        Assert.equal(gpv.balanceOf(msg.sender), expected, "Owner should have totalSupply Token initially");
    }

    function testInitialBalanceWithNewGrapevineToken() external {
        GrapevineToken gpv = new GrapevineToken();

        uint expected = gpv.totalSupply();

        Assert.equal(gpv.balanceOf(msg.sender), expected, "Owner should have totalSupply Token initially");
    }*/
}
